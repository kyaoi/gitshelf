pre-commit:
  commands:
    gofmt-staged:
      run: scripts/hooks/check_gofmt_staged.sh
    public-contract-docs:
      run: scripts/check_public_contract_docs.sh

pre-push:
  commands:
    heavy-checks:
      run: scripts/run_heavy_checks.sh
