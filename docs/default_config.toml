# gitshelf config
kinds = ["todo", "idea", "memo"]
statuses = ["open", "in_progress", "blocked", "done", "cancelled"]
link_types = ["depends_on", "related"]

default_kind = "todo"
default_status = "open"
